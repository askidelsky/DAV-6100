show databases;
use dav6100_db;

create table profiling(
`rank` int,
`title` varchar(80),
`artist` varchar(80),
`release` date,
`summary` varchar(80),
`metascore` int,
`userscore` varchar(80),
`link` varchar(80),
`img_url` varchar(80));